      <%= form_for(@cohort) do |c| %>

      <label> Cohort</label><br>
      <%= c.text_field :name %><br>

      <label>Start: </label><br>
      <%= c.number_field :start_date %><br>

      <label>End: </label><br>
      <%= c.number_field :end_date %><br><br>

      <%= c.submit "Save", {class: "btn btn-success" }%>
      
      <% end %>
      <br>
      <%= link_to "Back", :back %>